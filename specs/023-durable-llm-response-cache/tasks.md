# Tasks — Durable LLM response cache

- [x] **T1 — Migration** `db/migrations/versions/0034_llm_response_cache.py` (down_revision `0033_voice_guide`): real DDL for `llm_response_cache` — PK `(release_run_id, idempotency_key)`, `release_run_id` FK CASCADE, `response JSONB`, `task_name`/`model_id`/`input_tokens`/`output_tokens`/`created_at`, index on `created_at`. Clean inverse downgrade.
- [x] **T2 — Port + Aurora adapter** `LlmResponseCache` Protocol + `InMemoryLlmResponseCache` fake in `model_client.py` (pure); `AuroraLlmResponseCache` (psycopg) in `aurora_llm_cache.py` with `get` / `put` (ON CONFLICT DO NOTHING RETURNING + fallback SELECT) / `delete_older_than`.
- [x] **T3 — Wire two-tier cache** `BedrockModelClient` gains an optional `cache`; `generate_json` does L1 → L2 lookup, single Converse + meter on miss, L2 `put`, no re-meter on hit. `from_env` accepts `cache` + honors `LLM_RESPONSE_CACHE_DISABLED`. Pass `AuroraLlmResponseCache(conn)` at all five `__main__` `from_env` sites (release, content, media, skill, eval).
- [x] **T4 — Retention sweep** Pure `cache_sweep_cutoff` policy (window from `LLM_CACHE_TTL_DAYS`, default 30); `AuroraLlmResponseCache.delete_older_than`; `llm-cache-sweep` subcommand on the privacy CLI; step added to `retention-sweep.yml`. (GDPR run-erasure already covered by the CASCADE.)
- [x] **T5 — Tests** Unit: port-fake get/put/first-writer-wins/run-scoping; `generate_json` L1/L2 dedup + single `meter_call` on a repeat key + `cache=None` parity; sweep-cutoff math. Integration (`RUN_INTEGRATION=1`): Aurora adapter round-trip, two-instance cross-process dedup, cross-run isolation. ≥80% coverage on changed modules.

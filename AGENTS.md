# Workspace Agents (harness-managed)

Generated by `harness apply`. This file is auto-generated; **do not hand-edit.**
All three vendor CLIs (claude-code, codex-cli, gemini-cli) read this file on
session start to discover the agents declared in this project.

---

# Project memory — harness-managed

This file is auto-generated by `harness apply`. **Do not hand-edit.** Operator-
managed conventions live in `memory/constitution.md`; project setup in
`harness.cue`. This file just surfaces them to the active agent on every
session start.

## Active spec

`001-release-run-skeleton-trigger-run-lifecycle-dashboard` — a previous session set this marker and the post-pass gates have
not yet cleared it. Run `/spec-kit-implement 001-release-run-skeleton-trigger-run-lifecycle-dashboard` to resume.

## Available spec-kit slash commands

- `/spec-kit-implement <spec-id>` — Implement one spec end-to-end. Post-pass gates run automatically on session end.
- `/spec-kit-review` — Run the spec-kit review across the project (task coverage, PRD anchors, REVISIONs, wiring, params).
- `/spec-kit-progress` — Report task coverage across every spec; show BUILD COMPLETE when all cited.
- `/spec-kit-completeness` — Run HC1-HC5 structural detectors (unreachable modules, unused params, stub migrations, AC alignment, fixture shortfalls).
- `/spec-kit-handoff <spec-id>` — Prime the L1 event log + linkedSpec for a spec. Emit one task event per declared T-ID.

These are emitted into your vendor CLI's native surface automatically
(Claude Code: project skills under `.claude/skills/`; Gemini: custom
commands under `.gemini/commands/`). Invoke them with the hyphen form
shown above — project skills and flat command files do not colon-namespace.
Native session resume, remote control, and the vendor's other features work
unchanged — the harness's L0 broker, L1 event log, and cost telemetry fire via
the installed hooks regardless of how the session was launched.

## Codebase navigation

If `docs/CODEMAPS/` exists, read it first to orient before scanning files — it is a
token-lean architecture map. Run `/codemap` to (re)generate it after major
features or refactors.

## When you see `agent.bearings.drift-suspected`

Run `harness reground` before your next tool call. It returns one
deterministic summary of: the active feature, active spec, constitution
principles, protected paths, recent progress entries, and the cost
budget — built from the harness state on disk, no LLM tokens spent.

## Protected paths

The L0 broker will REFUSE writes to these paths without explicit operator
approval. Anticipate this — if you need to modify a protected path, surface
the requirement to the operator first.

- `.git/HEAD`
- `.harness/state/**`
- `.harness/traces.jsonl`
- `.harness/traces.jsonl.*`
- `.harness/grants.sqlite*`
- `memory/constitution.md`
- `specs/*/spec.md`
- `docs/PRD.md`
- `.env`

## Constitution (inlined from `memory/constitution.md`)

# Project Constitution

These are project-level invariants that hold across every feature and spec in the release-to-content engine. A spec may add constraints but may never relax one below. Violating any invariant here is a constitutional change requiring explicit operator approval — not a normal code review.

## 1. Substrate

- Languages: **TypeScript** (Next.js App Router + React 19 UI/API) and **Python** (LangGraph workers, extractors, capture). No third language without operator approval.
- Orchestration is **LangGraph** only — graph state, conditional routing, retries, and human-approval interrupts. No Step Functions, EventBridge, Bedrock Agents, or bespoke job schedulers.
- Long jobs run on the **GitHub Actions runner**; the **Vercel/v0** app hosts only the dashboard and thin API routes. UI never executes diff analysis, Playwright, or ffmpeg.
- Model + safety calls go through **Amazon Bedrock Converse API** and **Bedrock Guardrails** exclusively. No direct provider SDKs, no self-hosted LLM serving.
- The core invariant of the product: **never generate marketing/sales/demo content directly from raw diffs.** Build an evidence-backed feature manifest → human-approve → generate with claim-level provenance.

## 2. Scope

- In scope: GitHub-sourced release detection, evidence collection, deterministic signal extraction, feature clustering, human-gated content + media generation, skill self-learning ledger.
- Out of scope (non-goals — adding any is a constitutional change): full autopublishing without human approval, generalized/AI video generation, multi-VCS support beyond GitHub, Bedrock Knowledge Bases/Agents, Step Functions/EventBridge/Lambda/ECS, self-hosted models, statistical skill-promotion tests.
- Tenancy: **internal, single-org tool**. Every record is scoped to a `release_run_id`; cross-run data bleed is forbidden.
- Repo shape: canonical skills live in-repo as `skills/**/SKILL.md`. Aurora is a **staging/telemetry/provenance layer only** — it never becomes the source of truth for skills.

## 3. Primitives — use them; do not reinvent

| Primitive | What this project does NOT reimplement | What this project owns |
|---|---|---|
| LangGraph interrupts/checkpointer | approval gates, resume, retry, graph state machine | the 4 graphs and their node logic |
| Bedrock Converse | LLM transport, model invocation, throttle/retry semantics | prompts, model-tier routing, token budgets |
| Bedrock Guardrails | PII/sensitive-info filtering, output redaction policy | guardrail config + deterministic policy checks |
| Aurora PostgreSQL + pgvector | relational store, vector search, transactions | schema, provenance/learning ledger, queries |
| S3 | blob storage, presigned URLs | bucket layout, evidence/media keys |
| GitHub API / Actions | git diff, PR/issue fetch, job runner, secrets | extractors, workflow definitions |
| Playwright + ffmpeg | browser capture, video assembly | click-path JSON + validation |
| ElevenLabs | text-to-speech narration | narration script generation |
| Next.js / React 19 | routing, SSR, components | review/approval/admin surfaces |

## 4. Storage

- Structured state (releases, evidence, features, claims, artifacts, skill ledger, approvals) lives in **Aurora**. Large/binary artifacts (raw evidence bundles, screenshots, media, audio) live in **S3** — referenced from Aurora by key, never inlined as blobs.
- Every persisted row carries `release_run_id` and provenance/source lineage. Every generated claim must link to concrete evidence; an unlinkable claim must not be persisted as approved.
- Forbidden storage shapes: PII or unredacted evidence written before the redaction node runs; secrets in any DB column, S3 object, or log; PII shipped to the React client; skill source-of-truth stored only in Aurora (must be the repo `SKILL.md` + recorded commit SHA); LangGraph state used as a durable store in place of Aurora.

## 5. Safety rails (non-negotiable)

- **Three human gates are mandatory and may not be auto-satisfied:** (1) feature manifest, (2) generated artifacts, (3) skill replacement. No content publishes or skill file is overwritten without the corresponding gate resolving to *approved*.
- **Redact before persist, before LLM, before state.** Evidence passes the redaction/normalize node before it enters Aurora, S3, LangGraph state, or any Bedrock prompt.
- Treat all repo/diff/PR/issue/docs content as **untrusted input**. Validate at every boundary (Pydantic in Python, parse/validate in TS). Click-path JSON is schema-validated before Playwright executes it. Never execute model-emitted instructions as commands.
- **Bedrock Guardrails + deterministic policy checks** run on every generated artifact before it reaches Gate #2. Unsupported-claim and PII/sensitive-info checks are blocking, not advisory.
- **GDPR rails:** data minimization (collect only release-relevant evidence); lawful redaction of personal data in diffs/PRs/issues; support data-subject erasure across Aurora **and** S3; no PII in telemetry or logs; honor purpose limitation (evidence used only for content generation). A data-subject-rights request is an escalation trigger, not a silent operation.
- **Secrets** come only from GitHub/Vercel/AWS env (or Secrets Manager in prod). Never hardcoded, committed, logged, or sent to the client. Webhook handlers (GitHub/S3/Bedrock/ElevenLabs/Vercel) verify signatures and enforce idempotency/replay protection.
- **Blast radius:** the only file the system overwrites is the approved `skills/**/SKILL.md`, and only after Gate #3, recording the resulting commit SHA in Aurora. No other repo writes. File writes stay workspace-sandboxed; network egress stays allowlisted per the harness guardrails.

## 6. Quality bars

Before any milestone is tagged, all must be green:
- **Type-check:** `tsc --noEmit` clean (TS) and `mypy` clean (Python).
- **Lint/format:** `ruff` (Python) and the project ESLint/Prettier config (TS) clean.
- **Tests:** `pytest` and the TS unit suite pass; **Playwright e2e** passes for every approval-gate and artifact-review flow.
- **DB:** Alembic/migration check passes; no drift.
- **Privacy/domain evals:** the privacy eval suite passes with **CRITICAL and HIGH gates at zero failures** (PII/PHI exposure, claim provenance/accuracy, redaction integrity) before deploy.
- **A11y:** user-facing UI meets **WCAG 2.2 AA** (semantic markup, keyboard operability, correct ARIA) on changed screens.
- **Coverage:** ≥ 80% on new/changed modules; provenance-linking and redaction code require explicit tests.
- **Cost/latency:** LLM pipeline stays within its token/latency budget eval gate; no untracked model-tier upgrades.

## 7. Escalation contract

An autonomous session MUST pause and ask a human when:
- Any of the three approval gates is reached — never self-approve, self-edit-and-approve, or skip a gate.
- A redaction or Guardrails/PII check fails, is ambiguous, or would be bypassed.
- A generated claim cannot be linked to concrete evidence.
- A change would touch scope/non-goals (§2), add a service/dependency, alter storage shape, or modify a webhook/secret/IAM boundary.
- A GDPR data-subject request (erasure/access) or any personal-data handling decision arises.
- The skill-learning graph would overwrite a repo `SKILL.md` (Gate #3) or promote a candidate.
- Quality bars (§6) cannot be met, or a fix would require weakening a safety rail.
- Tests/migrations are red and the fix is non-obvious, or required credentials/permissions are missing.

## 8. Definition of done

v1.0 is shipped only when:
- The full loop runs end-to-end: release trigger → evidence collect/redact/persist → deterministic signals → feature clustering → **Gate #1** → artifact generation (blog/changelog, sales one-pager, social snippets, demo script, audio digest, optional demo video) → claim extraction + evidence linking → deterministic checks + Guardrails → **Gate #2** → optional media (Playwright + ffmpeg + ElevenLabs → S3) → learning signals → skill candidate → **Gate #3** → repo `SKILL.md` replaced with commit SHA recorded in Aurora.
- Every shipped artifact has claim-level provenance traceable to evidence; nothing publishes without its gate.
- All §6 quality bars green on `main`; privacy evals pass CRITICAL/HIGH.
- Dashboard supports release review, feature approval, artifact review, and skill-proposal approval, all keyboard-operable and WCAG 2.2 AA.
- No secrets in code/logs/DB/client; redaction verified on a real release run; GDPR erasure verified across Aurora + S3.
- Runs reproducibly on the GitHub Actions runner with documented env/secrets; no deferred non-goal silently introduced.

## Project rules (harness-bootstrap)

Standing rules provisioned by `harness rules bootstrap` — MUST/NEVER defaults for this project. Each entry lists the directive only; read the linked file for rationale, detail, and sources.

### Aurora PostgreSQL integration rules
_full text: `memory/rules/aurora-postgresql-rules.md`_

- Secrets — : NEVER hardcode DB credentials or commit connection strings.
- Prefer IAM auth in prod
- Least privilege — : give the app role only the rights it needs (no superuser/`rds_superuser`); use a separate, narrower role for read-only/reader-endpoint…
- TLS is mandatory
- Connections — : from short-lived contexts (GitHub Actions jobs, Next.js serverless/route handlers) do NOT open raw per-invocation pools to the cluster — route…
- Failover-safe DNS
- Idempotent writes
- Event "webhooks" (RDS events via SNS → Next.js routes)
- Replay protection
- pgvector / data hygiene

### AWS Bedrock integration rules
_full text: `memory/rules/aws-bedrock-rules.md`_

- Credentials via IAM roles, never static keys.
- Least-privilege IAM scoped to ARNs.
- Call the Converse API, not model-specific InvokeModel payloads.
- Always attach a published Guardrail.
- Guardrails are not the only check.
- Handle ThrottlingException with backoff.
- Idempotency: synchronous Converse has none — the app must dedupe.
- Webhooks: Bedrock has no native outbound webhook.
- Webhook idempotency + auth.
- Separate sandbox vs prod.
- Protect data in transit and at rest.
- Don't log prompts/outputs/credentials.

### Coding standards
_full text: `memory/rules/coding-standards.md`_

- Prefer many small, cohesive files over few large ones; keep functions short and single-purpose.
- Name for intent: descriptive variables/functions; boolean names read as predicates (is/has/should).
- Favor immutability — return new values instead of mutating shared state where practical.
- KISS / DRY / YAGNI: simplest thing that works, extract real duplication, don't build for imagined futures.
- Validate inputs at system boundaries; never trust external data (user input, API responses, file contents).
- Handle errors explicitly at every layer; never silently swallow them.
- Match the surrounding code's style, idioms, and formatter/linter config — don't introduce a personal style.

### Dependency policy
_full text: `memory/rules/dependency-policy.md`_

- Prefer the standard library and already-present dependencies before adding a new one.
- Justify each new dependency: what it does that existing code can't, and its maintenance/security cost.
- Pin versions; avoid floating ranges.
- Vet new dependencies: maintenance status, transitive footprint, license, known advisories.
- Prefer well-maintained, widely-used libraries over abandoned or single-maintainer ones for critical paths.
- Don't vendor or copy-paste library code to dodge the dependency process — surface the tradeoff instead.

### GDPR compliance & data-safety rules
_full text: `memory/rules/domain-gdpr-rules.md`_

- NEVER log, print, or persist raw personal data.
- Minimize at the source.
- Set retention/TTL on everything that can hold PII
- Pseudonymization ≠ anonymization
- Run Bedrock Guardrails PII filters on both Converse inputs and outputs
- Keep PII off the client.
- Encrypt PII in transit (TLS) and at rest (e.g. AES-256).
- Honor data-subject rights in the data model.
- No non-essential cookies, tracking, or fingerprinting without prior opt-in consent.
- Establish and record a lawful basis + purpose
- Use synthetic/fixture data in Playwright
- Return user-safe error messages

### ElevenLabs integration rules
_full text: `memory/rules/elevenlabs-rules.md`_

- NEVER expose `xi-api-key` to the browser: no `NEXT_PUBLIC_*` var, v0/React client component, Playwright page context, or committed config.
- MUST read the key at call time from env (`process.env.ELEVENLABS_API_KEY` / `os.environ["ELEVENLABS_API_KEY"]`) and fail fast with a clear error if missing.
- MUST treat `voice_id`, `model_id`, and `output_format` as config, not hardcoded constants; confirm current model IDs against the live API reference rather than…
- TTS has **no idempotency-key header** — enforce it yourself: compute a deterministic content hash of `(approved text + voice_id + model_id + output_format)`…
- MUST bound concurrent TTS calls below the subscription tier's concurrency cap (Free 2 / Starter 3 / Creator 5 / Pro 10 / Scale 15).
- On **429, branch on the error `code`** — do not blanket-retry: `rate_limit_exceeded` → exponential backoff + jitter (cap ~32s); `concurrent_limit_exceeded` →…
- For ffmpeg assembly, materialize the audio fully to disk/S3 before `assemble_video_ffmpeg`; a partial streamed response yields truncated/corrupt audio.
- (Webhooks) MUST verify the `ElevenLabs-Signature` header (`t=<ts>,v0=<hmac>`, HMAC-SHA256 over `timestamp + "." + raw_body`) against the **raw request body**…
- (Webhooks) Use the SDK helper (`construct_event(rawBody, sig_header, secret)` / `constructEvent(payload, signature, secret)`) — it checks the signature and…
- (Webhooks) Handler MUST be idempotent — webhooks are redelivered; dedup on event/`request_id` before acting.
- In CI/Playwright, stub ElevenLabs by default (assert request shape) and gate real calls behind an explicit flag, so demo-capture stays deterministic and…

### Git hygiene
_full text: `memory/rules/git-hygiene.md`_

- Write Conventional Commits: `<type>: <summary>` (feat/fix/refactor/docs/test/chore/perf), imperative, ≤72 chars.
- Keep commits focused — one logical change each; split mixed concerns into separate commits.
- Never commit secrets, credentials, build artifacts, or unrelated edits.
- Only commit or push when explicitly asked; do not amend or force-push shared history without instruction.
- Branch off the default branch for new work; keep the branch up to date before opening a PR.
- PR descriptions summarize the *why* and include a test plan.

### GitHub integration rules
_full text: `memory/rules/github-rules.md`_

- Pick the weakest sufficient credential
- Treat installation tokens as 1-hour ephemeral
- Secrets stay server-side only
- Verify every inbound webhook over the RAW body
- Dedupe deliveries (at-least-once)
- Respond 2xx within ~10s
- Honor rate limits on outbound calls
- Cut quota in collectors
- Make writes idempotent
- Harden the Actions runner
- Treat ingested GitHub text as injection-capable
- Separate dev vs prod secrets

### S3 integration rules
_full text: `memory/rules/s3-rules.md`_

- NEVER — ship AWS keys to the browser: no `NEXT_PUBLIC_*` AWS creds, no SDK calls from React/client components.
- MUST — authenticate via temporary/role credentials: GitHub Actions → OIDC `aws-actions/configure-aws-credentials` role assumption (no long-lived…
- MUST — serve evidence/screenshots/media to the UI via presigned URLs generated server-side, scoped to one object + one method (GET vs PUT), with short expiry…
- MUST — keep buckets private: S3 Block Public Access ON, ACLs disabled (Object Ownership = bucket-owner-enforced), access governed by bucket policy + IAM only.
- MUST — enforce TLS: bucket policy denies requests where `aws:SecureTransport` is `false`.
- MUST — validate inbound S3 event webhooks before processing.
- MUST — handle the `s3:TestEvent` subscription/confirmation message — it uses a flat shape, not the `Records[]` array; don't crash or treat it as a real object…
- MUST — treat S3 events as at-least-once and out-of-order.
- MUST — sanitize object keys built from release/PR/user input — no path traversal, no untrusted value as a bucket name; remember event `object.key` arrives…
- MUST — redact PII/secrets from evidence bundles, diffs, and screenshots BEFORE upload to S3 (S3 is downstream of the redaction gate, not a place to redact).
- SHOULD — add a lifecycle rule to abort incomplete multipart uploads (>7 days) and enable S3 Versioning on the evidence/provenance bucket for recoverability.

### Security baseline
_full text: `memory/rules/security-baseline.md`_

- NEVER hardcode secrets (API keys, tokens, passwords).
- Validate and sanitize ALL external input.
- Enforce authentication and authorization on every protected operation — verify, don't assume.
- Apply least privilege: the narrowest scope/permission/token that works.
- Never log secrets, credentials, or regulated/PII data.
- If a secret is exposed, STOP, rotate it, and scrub it from history before continuing.
- Pin and audit dependencies; treat a new dependency as new attack surface.

### Python coding rules
_full text: `memory/rules/stack-python-rules.md`_

- MUST keep `ruff check` and `ruff format` clean before declaring work done; `ruff format` owns whitespace/quotes/line-wrapping (defaults: line-length 88, double…
- MUST treat Ruff's default `["E4","E7","E9","F"]` as a floor; this repo expects `I` (import sort), `B` (bugbear), `UP` (pyupgrade), and `SIM` enabled in…
- NEVER use mutable default arguments (`def f(x=[])`, `={}`) or evaluate calls in defaults (B006/B008); default to `None` and build the value inside the body.
- MUST annotate all function signatures and Pydantic/LangGraph state models; use builtin generics (`list[str]`, `dict[str,int]`) and `X | None` over…
- MUST validate all external/boundary data (API routes, Bedrock/LLM output, GitHub payloads, DB rows) through Pydantic v2 models — never hand-roll dict…
- In `async def` code (LangGraph nodes, API handlers) NEVER call blocking I/O — no `time.sleep`, `requests`, sync DB drivers, or plain `open()` (ASYNC2xx); use…
- MUST compare to `None`/`True`/`False` with `is`/`is not` (E711/E712) and test emptiness via truthiness (`if not items:`), not `== None` or `len(x) == 0`.
- NEVER use bare `except:` or catch blind `Exception` to swallow errors (E722/BLE001); catch the narrowest type and re-raise with `raise NewError(...) from err`…
- MUST log with lazy `%`-style args (`logger.error("failed %s", id)`), NOT f-strings inside logging calls (G004); reserve f-strings for non-logging string…
- MUST use `pathlib.Path` over `os.path`, and acquire files/connections/locks via `with` context managers — never leave handles or DB sessions dangling.
- NEVER use wildcard (`from x import *`) or relative-walk imports; use absolute imports and let Ruff's `I` rule own ordering/grouping.
- MUST keep secrets and config out of source — read Bedrock/GitHub/Aurora/ElevenLabs credentials from env (and the harness secret broker), never hardcode or log…

### TypeScript coding rules
_full text: `memory/rules/stack-typescript-rules.md`_

- NEVER use `any`; use `unknown` for genuinely-unknown values and narrow via `typeof`/`in`/discriminated unions before use.
- MUST handle every Promise: `await` it, `return` it, or `void` it.
- Prefer `??` over `||` for defaulting (so `0`/`""`/`false` survive) and optional chaining `a?.b?.c` over `&&` chains.
- Use `import type { … }` / `export type { … }` for type-only imports so they're erased from the bundle and don't trigger side-effects…
- Prefer `satisfies` to validate an object against a type while keeping its narrow inferred literal types; use explicit annotations only when you need widening.
- Type function inputs and exported/public return types explicitly; let local returns infer.
- Model variant data as discriminated unions (shared literal `kind`/`type` field) and switch on it; add a `never`-typed `default` for exhaustiveness so new…
- Use `unknown` in `catch` (don't assume `Error`); narrow before accessing `.message`.
- NEVER use `@ts-ignore`; if unavoidable use `@ts-expect-error` with a one-line reason so it errors once the underlying issue is fixed.
- Prefer `readonly` arrays/props and `as const` for literal config/tuples; avoid mutating shared state.
- In React/Next.js: type props with explicit `interface`/`type` (no `React.FC` for children-implicit typing); type `useState`/`useRef` generics when inference is…
- Use lowercase primitives (`string`, `number`, `boolean`, `object`), never the boxed `String`/`Number`/`Object`.

### Testing discipline
_full text: `memory/rules/testing-discipline.md`_

- Every behavior change ships with tests — add or update them in the same change.
- Structure tests Arrange–Act–Assert; name them after the behavior under test.
- Test behavior through real entry points, not private implementation details.
- NEVER weaken or delete an assertion just to make a test pass — fix the code or the test's premise.
- Run the relevant tests before declaring done; report failures verbatim, don't paper over them.
- Cover the edge cases that matter (empty, boundary, error paths), not just the happy path.
- Keep tests fast and deterministic; no reliance on wall-clock, network, or order between tests.

### React UI rules
_full text: `memory/rules/ux-react-rules.md`_

- Keep components and Hooks pure: given the same props, state, and context they MUST return the same JSX.
- NEVER mutate props, state, context, or any value already passed to a Hook or used in JSX.
- Call Hooks ONLY at the top level of a component or custom Hook — never inside conditions, loops, nested functions, or after an early return.
- Derive, don't store: compute values from existing props/state during render instead of mirroring them into `useState` + an Effect.
- Use `useEffect` ONLY to synchronize with external systems (subscriptions, non-React widgets, data fetch with cleanup to avoid race conditions).
- Reset a child's state by passing a changed `key`, not by clearing it in an Effect.
- Give every list item a stable key derived from data (e.g. an id); NEVER use the array index as key for lists that can reorder, filter, or grow.
- Mark interactive/stateful components with `"use client"`; keep them small and leaf-level, and keep data fetching, secrets, and heavy logic in Server Components…
- Use semantic HTML (`<button>`, `<a>`, `<nav>`, `<label>`, headings) instead of `<div onClick>`; native elements provide role, keyboard operability, and focus…
- Associate every form control with a `<label>` via `htmlFor`/`id` (JSX uses `htmlFor` and `className`, not `for`/`class`); give meaningful `alt` text to images…
- Ensure all interactive UI is keyboard-operable with a visible focus indicator, and move focus deliberately on route changes, dialog open/close, and async…

### Vercel integration rules
_full text: `memory/rules/vercel-rules.md`_

- MUST verify every inbound Vercel webhook/log-drain request
- MUST sign over raw bytes, not parsed JSON.
- NEVER hardcode the signing secret.
- MUST make webhook handlers idempotent.
- MUST return 2xx quickly
- NEVER expose secrets to the browser.
- MUST verify signatures in the Node runtime, not Edge.
- MUST scope env vars per environment
- Vercel REST API: MUST authenticate with `Authorization: Bearer <token>`
- MUST keep Playwright preview/production access scoped


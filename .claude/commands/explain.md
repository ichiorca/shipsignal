---
description: explain the named file/symbol — what it does, how it fits, and any risks
argument-hint: [file or symbol]
---

Explain the following file or symbol: `$ARGUMENTS`

Steps:
1. Locate the target (file, function, type, or symbol). If ambiguous, pick the best match and say which.
2. Summarize in 2–3 sentences: what it does and why it exists.
3. Walk the important parts — control flow, key data structures, and any non-obvious logic.
4. Explain how it fits the wider codebase: who calls it, what it depends on, what it produces.
5. Flag risks: edge cases, error handling gaps, performance traps, or surprising behavior.

Prefer concrete references (`file:line`) over generalities. Keep it tight.

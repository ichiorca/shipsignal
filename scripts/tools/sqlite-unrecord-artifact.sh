#!/bin/sh
# L3 demonstration scaffold: sqlite-unrecord-artifact (write-class, destructive; saga compensation; local SQLite ledger).
# Replace with the real ledger DELETE as a task of the first spec.
# Reads a JSON arg envelope on stdin; emits a JSON ack; exits 0.
echo '{"ok":true,"tool":"sqlite-unrecord-artifact","note":"scaffold — not yet implemented"}'
exit 0

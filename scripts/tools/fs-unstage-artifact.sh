#!/bin/sh
# L3 demonstration scaffold: fs-unstage-artifact (write-class, destructive; saga compensation; local FS).
# Replace with the real staging delete as a task of the first spec.
# Reads a JSON arg envelope on stdin; emits a JSON ack; exits 0.
echo '{"ok":true,"tool":"fs-unstage-artifact","note":"scaffold — not yet implemented"}'
exit 0

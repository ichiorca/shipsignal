#!/bin/sh
# L3 demonstration scaffold: fs-stage-artifact (write-class; idempotent; saga forward; local FS).
# Replace with the real staging write as a task of the first spec.
# Reads a JSON arg envelope on stdin; emits a JSON ack; exits 0.
echo '{"ok":true,"tool":"fs-stage-artifact","note":"scaffold — not yet implemented"}'
exit 0

#!/bin/sh
# L3 demonstration scaffold: read-source-file (read-class, cached, local).
# Replace with the real read as a task of the first spec.
# Reads a JSON arg envelope on stdin; emits a JSON ack; exits 0.
echo '{"ok":true,"tool":"read-source-file","note":"scaffold — not yet implemented"}'
exit 0

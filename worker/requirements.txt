# T5 (spec 001) — runtime deps for the LangGraph release worker on the Actions runner.
# Pinned (dependency-policy: no floating ranges). The unit-test gate does NOT install
# these — it imports only the pure modules (status/state/nodes/repository); these are
# the heavy libs behind graph.py / aurora_repository.py used at deploy time.
langgraph==0.2.60
psycopg[binary]==3.2.3
# T1 (spec 017) — durable LangGraph checkpointer over Aurora (PRD §3.1/§5.6). PostgresSaver
# ships in a SEPARATE package from langgraph core; without it build_checkpointer() raises
# ModuleNotFoundError on the production (DATABASE_URL) path and the run is marked failed.
# psycopg-pool backs PostgresSaver's connection pool (psycopg itself is pinned above). Versions
# target the langgraph 0.2.x line; re-confirm when bumping langgraph.
langgraph-checkpoint-postgres==2.0.9
psycopg-pool==3.2.4
pydantic==2.10.4
# boto3 backs the Bedrock Converse model client (spec 004) and the S3 evidence sink
# (spec 002). Only the runtime adapters import it; the unit gate does not install it.
boto3==1.35.90
# playwright drives the demo-media capture (spec 008, T3). Lazy-imported inside
# PlaywrightDemoCapturer.capture so module load never requires the browser dep; the unit
# gate uses the in-memory capturer instead. ElevenLabs TTS (T4) and ffmpeg (T5) need no
# Python dep — narration goes over the v1 REST API via urllib (stdlib), and ffmpeg is the
# system binary invoked through subprocess on the Actions runner.
playwright==1.49.1
